server:
    port: 8760
eureka:
    client:
        service-url:
            defaultZone: http://localhost:8760
        register-with-eureka: false
        fetch-registry: false
     instance:
         hostname: localhost
         prefer-ip-address: true
